# 工作记录 · 2026-08-01

阳光云月度人力核算从手工 Excel 迁移到工作台，本文记录做了什么、为什么这么做、发现了哪些原表问题。

## 背景

原先用 `云平台-YYYY年M月人力情况.xlsx` 手工核算：三个组主管填人头数 → PM 贴 Teambition 导出 → SUMIFS 交叉汇总 → 算产能偏差。痛点是每月催填、手工搬数据、公式随组织调整要改列，且全靠 PM 一人维护。

## 交付内容

`人力产能工作台/` 目录，六个页签对应填报流程：版本周期 → 人头数 → 专项锁定 → 工时导入 → 迭代口径 → 偏差分析。

| 文件 | 职责 |
|---|---|
| `config.js` | 组织架构与业务配置。组织调整只改这里 |
| `calc.js` | 纯计算层，无 DOM 依赖，可直接复用到后端 |
| `parse.js` | CSV/xlsx 解析，自动判别文件类型 |
| `sync.js` | 协同同步层，自动判别单机/服务端模式 |
| `app.js` | 界面与状态管理 |
| `server.js` | 可选的协同服务端，零 npm 依赖 |

三种用法共用同一份代码：双击 HTML（单机）、`node server.js`（本地共享）、服务器部署（长期）。详见 `部署指南.md`。

## 关键判断与权衡

**1. 为什么只导入一个工作量统计表**

用户最初认为「中后台工时 = 两个表中涉及中后台团队的和值」。逐键对账后发现：`阳光云迭代工作量统计` 与 `阳光云迭代中后台工作量统计` 各 737 行，**键完全一致，仅 1 处数值差异**（后端开发-平台/7月C版本：172.5 vs 170.5）。两者是同一份数据的两个导出视图，区别仅在于前者多「预估故事点」列。

两表相加会让中台工时翻倍。中台各组工时本就挂在 `中后台-2026年M月迭代` 下，勾选该迭代即完整取到。故只导入前者（超集）。

**2. 为什么必须引入「迭代」维度**

原 Excel 基于手工筛选后的干净数据，而原始导出含 **116 个迭代、87 个团队**（站控、嵌入式、ECO、工具开发部等全在内）。不按迭代筛选就是一团乱账。因此迭代成为一等筛选条件，并支持多选以应对跨月合并。

**3. 为什么未识别的团队/分类要显式报错**

Excel 的 SUMIFS 匹配不到就静默返回 0，组织调整时错误无声无息。工作台改为在导入页红字列出未识别项并说明其工时未被计入 —— 刻意的显式失败。

**4. 为什么产能偏差用总计表而非看板**

看板缺 ECO 组数据、且与总计表有 0.5~1.5 人天的零星差异（导出时间差）。总计表是超集，作为权威口径；看板仅用于产线分布，并自动跑对账把超阈值差异列出。

**5. 协同方案为何需要后端**

纯前端 + localStorage 的数据只存在各人浏览器里，分享 HTML 文件等于各填各的，「实时看到更新」物理上做不到。故加 `server.js`：SSE 推送实现实时刷新，乐观锁防覆盖，原子写盘防并发读半截 JSON，每次变更自动归档。

## 发现的原表问题

1. **`规划与产能分析!B30` 是硬编码值 293.1，不是公式。** 同列其他行都是 `=D16` 形式引用，唯独后端开发-阳光云这行被字面值覆盖过。Python 独立复算 SUMIFS 实为 **429.6**，长期少算 136.5 人天 —— 该组产能缺口被显著低估（按 293.1 算超出 111.1，实际超出 247.6）。

2. **团队名大小写不一致。** 导出写 `WEB开发-阳光云`，汇总表头写 `Web开发-阳光云`。Excel 的 SUMIFS 不区分大小写所以未暴露，改成代码后必须显式归一化，否则该列静默为 0。已在 `calc.js` 的 `normTeam()` 处理。

3. **「迭代剩余」合计语义不清。** sheet1 的 `W14 = SUM(W12:W13)`（项目投入 + 迭代剩余），若 W13 已是净可投入人数，该合计存在重复计数嫌疑。工作台未照搬：第②页直接填可投入净人数，专项锁定单独登记只做核对。

## 数据质量问题（工具能暴露但修不了）

**「智慧能源产品中心」占约 67% 工时**（1831.75 / 2781.3 人天）。这是部门级兜底分类而非真实产线，占比这么高说明大量任务未打产线标签，产线维度分析（户用 577.95、储能 224.6、工商业 50）实际只覆盖小头。

这需要在 Teambition 建任务时把产线字段填对，属流程问题。如果产线分布是汇报重点，推动这件事比优化工具更有价值。

## 验证情况

用 2026 年 7 月真实 CSV 端到端跑通：

```
总计表 551 行 / 看板 62 行 → 未识别团队 0，未归类分类 0，对账差异 0 处
总计口径 2781.3 人天 | 看板口径 2784.3 人天（差 3 人天，为已知舍入级差异）
```

服务端验证：读写正常、乐观锁正确拒绝过期 rev（409）、SSE 广播 1 秒内到达、`data/` 目录访问被拒（403）、路径穿越（含 URL 编码）被拒、中文 UTF-8 正确落盘、自定义端口与数据目录生效、口令模式与 Cookie 免重复带参生效。

## 后续待办

- [ ] 用 7 月数据核对第⑥页汇总表与原 Excel（B30 那行以工作台为准）
- [ ] 口径确认后发给主管试填一个月
- [ ] 组织调整时改 `config.js` 的 `TEAMS`，不要动其他文件
- [ ] 推动 Teambition 建任务时填产线字段，降低「智慧能源产品中心」兜底占比
- [x] ~~Teambition API 对接~~ —— 需申请开发权限，成本过高，2026-08-01 决定放弃，长期用手动导入

---

# 补充记录 · 2026-08-03

本次解决三件事：导入语义、迭代清单刷新、服务中断时的数据安全。前两件是产品行为调整，第三件推翻了原先的冲突处理设计。

## 1. 导入即覆盖，不留历史

数据源每月重导一次，导入就是替换，不做增量与版本留存。界面上把「已导入」状态做实：卡片左侧绿边、绿框展示文件名/行数/导入时间，下方投放区明确写「覆盖当前数据」，避免误以为是追加。

## 2. 迭代清单全量重建

原逻辑保留旧勾选、新迭代默认不选。改为**完全跟随新数据重建，全部取消勾选**，理由是清单必须与当前导入文件严格对应，否则会残留上月迭代且难以察觉。

代价是分两次导入（工作量统计 + 人力看板）时，第二次会清掉第一次的勾选。用 `state.iterDirty` 标记，在第⑤页顶部红字提示「清单已重建，请重新勾选」，勾任意一项后提示自动撤下。相比静默保留旧勾选，显式提醒更安全。

## 3. 服务中断：从「二选一」改为「三方合并」

### 原设计的缺陷

原方案是断连暂存 + 恢复时弹窗二选一（用我的 / 用他的）。这个设计在**多人同时离线**时必然丢数据：

```
服务在 rev 10 关闭
  A 离线填自己组 → 本机快照（含A，基于 rev10）
  B 离线填自己组 → 本机快照（含B，基于 rev10）
服务重启
  A 先恢复 → 无人动过，自动补交 → rev 11（含A）
  B 后恢复 → 发现 rev 变了 → 弹窗二选一
       选「用我的」→ 覆盖成 B 的快照，A 的填写丢失
       选「用他的」→ B 离线填的全白填
```

**根因**：冲突判断粒度是「整份数据」，而实际填报粒度是「每人各自的几行」。粒度错配把本可自动合并的场景降级成了二选一。

### 现设计

以「断连瞬间的服务端快照」为基线，分别算出我方与服务端各自相对基线改了什么，按业务粒度逐项叠加：

| 数据 | 合并粒度 | 说明 |
|---|---|---|
| 人头数 | 按组 | 各组填各组，天然不撞车 |
| 专项锁定 | 按条目名 | 任一方删除即视为删除 |
| 迭代勾选 | 按迭代名 | 各勾各的可合并 |
| 版本周期 | 整体 | 行内字段相互关联，不宜拆开 |
| 工时源数据 | 整体 | 谁后导入以谁为准，迭代清单强制跟随同一来源 |

只有一方改过 → 采用那一方；两方改成不同值 → 保留我方并记入冲突清单。**不弹窗打断**，改为顶部黄条列出「这 N 处两边都改过，已暂用你的，请核对」—— 两边填写都已保住，不存在必须当场做的取舍。

按你们的分工（各组主管填自己那行，导入与专项由 PM 自己弄），撞同一格概率极低，实际几乎全自动。

## 4. 顺带修掉的五个丢数据场景

排查中发现的，都会真丢数据：

1. **服务端 `state.json` 损坏时静默返回空态。** 写盘中途断电可致。各端会把空数据当最新版，一提交就抹掉所有人填写。改为自动回退到 `history/` 里 rev 最大的可用快照；无快照时另存 `.broken` 供人工检查。
2. **rev 倒退导致永久锁死。** `data` 目录被删或回滚快照后服务端 rev 变小，客户端 rev 更大 → 每次提交都 409 → 永远提交不上。乐观锁改为「仅当客户端版本比服务端旧时才拒绝」，倒退时放行并接续客户端版本号。
3. **服务端返回 5xx 时数据无处可去。** 磁盘满、权限错误等，原代码只当普通失败提示，改动丢失。改为与网络失败同等对待：暂存本机 + 转入离线态。
4. **输入框失焦前关页面。** `change` 事件要失焦才触发，正在敲的那格没提交。`beforeunload` 里强制触发一次 change。
5. **localStorage 也存不下时无人知晓。** 配额满或隐私模式，数据只在内存里，刷新即失。加 `onStoreFail` 回调弹窗提示立即导出 JSON。

## 5. Windows 上 Ctrl+C 的处理

服务端加了优雅关闭：Ctrl+C 时先给所有在线页面推 `shutdown` 事件再退出，各端立刻转入本机暂存，不必等 TCP 超时（可能几十秒，这期间填写会静默丢失）。

Windows 的 Git Bash / MinTTY 下 Ctrl+C 不一定能转成 SIGINT，故额外用 `readline` 兜一层，仅在交互式终端启用。即便两层都没捕获、进程被硬杀，各端仍会通过 SSE 断连在数秒内察觉——主动通知只是让感知更及时，不是数据安全的唯一依赖。

## 验证

新增两个回归测试，改代码后直接跑：

```
node _test-merge.js     三方合并 9 个场景 20 项断言，全通过
node _test-server.js    服务端 6 组 17 项断言，全通过
```

覆盖：多人离线各填各的不丢、三人叠加、真撞车报冲突、一方改一方没动不误报、迭代勾选合并、重导入时清单跟随、专项增删、基线缺失降级、乐观锁、rev 倒退放行、shutdown 通知、重启保留数据、损坏回退快照、`.broken` 留存。

测试脚本用 `_` 前缀，与业务代码区分，部署时可不传。

**注**：Windows 上 `child.kill('SIGINT')` 走 TerminateProcess 会直接杀进程、不触发 handler，无法用它验证优雅关闭。测试改用 wrapper + IPC 触发真实 handler，避免往 `server.js` 里塞测试专用代码。
