# 部署指南

三种用法，代码完全相同，**不用改任何文件**，区别只在启动方式。

| 方式 | 适合场景 | 别人能一起填吗 |
|---|---|---|
| A 单机 | 只有你自己核算 | 否 |
| B 本地共享 | 临时试用、几个人短期协作 | 能，但你的电脑必须开着 |
| C 服务器部署 | 长期固定使用 | 能，7×24 可用 |

---

## A. 单机模式

双击 `index.html`。数据存浏览器本地，顶栏显示「单机模式」。

需要给别人看时用「导出 JSON」，对方「导入 JSON」。适合你一个人先把口径跑通。

---

## B. 本地共享（你现在用的）

```bash
cd 人力产能工作台
node server.js
```

控制台会打印同事访问地址，发给他们即可。你关机或断网，服务就停了。

改端口（默认 8770 被占用时）：

```bash
node server.js 9000
```

---

## C. 部署到内网服务器

### 前置：找运维确认三件事

1. 服务器操作系统（Linux 还是 Windows Server），决定后面用哪套命令
2. 能否装 Node.js（要 v14 以上，`node -v` 查看）
3. 开放哪个端口，且该端口在公司内网可访问

### 第 1 步：传文件

把整个 `人力产能工作台` 目录传到服务器，比如 `/opt/capacity`（Linux）或 `D:\capacity`（Windows）。

**不要传 `data` 目录** —— 那是你本地的测试数据，服务器上会自动重建。

### 第 2 步：装 Node.js

Linux（CentOS/RHEL）：
```bash
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs
node -v
```

Linux（Ubuntu/Debian）：
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo bash -
sudo apt install -y nodejs
node -v
```

Windows Server：到 nodejs.org 下载 LTS 版 msi，双击安装。

### 第 3 步：先手动跑一次，确认能通

```bash
cd /opt/capacity
node server.js
```

浏览器打开 `http://服务器IP:8770`，能看到界面就说明通了。按 `Ctrl+C` 停止，进入下一步做成常驻服务。

**打不开的话**，按顺序排查：

- 服务器本机 `curl http://localhost:8770` 能通吗？不通 = 服务没起来，看控制台报错
- 能通但外部访问不了 = 防火墙。Linux：`sudo firewall-cmd --add-port=8770/tcp --permanent && sudo firewall-cmd --reload`；Windows：高级安全防火墙里加入站规则放行 8770
- 还不行 = 找运维确认网络策略是否限制该端口

### 第 4 步：做成开机自启的常驻服务

#### Linux（systemd，推荐）

新建 `/etc/systemd/system/capacity.service`：

```ini
[Unit]
Description=Cloud Capacity Workbench
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/capacity
ExecStart=/usr/bin/node /opt/capacity/server.js
Restart=always
RestartSec=5
Environment=PORT=8770
Environment=DATA_DIR=/opt/capacity/data
# 需要访问口令时取消下面这行注释，换成自己的口令
# Environment=ACCESS_TOKEN=换成你的口令

[Install]
WantedBy=multi-user.target
```

启用：

```bash
sudo systemctl daemon-reload
sudo systemctl enable capacity
sudo systemctl start capacity
sudo systemctl status capacity     # 确认 active (running)
```

日常操作：

```bash
sudo systemctl restart capacity    # 重启
sudo journalctl -u capacity -f     # 看实时日志
```

#### Windows Server（用 nssm 装成服务）

下载 nssm（nssm.cc），管理员命令行执行：

```cmd
nssm install Capacity "C:\Program Files\nodejs\node.exe" "D:\capacity\server.js"
nssm set Capacity AppDirectory D:\capacity
nssm set Capacity AppEnvironmentExtra PORT=8770 DATA_DIR=D:\capacity\data
nssm start Capacity
```

### 第 5 步：通知同事

把 `http://服务器IP:8770` 发到群里。第一次打开会让填姓名（用于记录填报人），之后每人只填自己那几行，**谁提交别人页面都会自动刷新**。

---

## 可选配置

三个环境变量，**都不填就是默认值**，本地用完全不需要设置。

| 变量 | 默认 | 说明 |
|---|---|---|
| `PORT` | 8770 | 监听端口 |
| `DATA_DIR` | `./data` | 数据目录。部署时建议指向独立路径，便于备份 |
| `ACCESS_TOKEN` | 空（不校验） | 访问口令 |

设了口令后，分享的链接要带参数：`http://服务器IP:8770/?token=你的口令`。同事点一次后写入 Cookie，之后刷新不用重复带参数（有效期 30 天）。

启动时命令行会直接打印带口令的完整链接，复制即可。

---

## 数据备份

所有数据在 `DATA_DIR` 目录下：

- `state.json` — 当前数据
- `history/` — 每次变更的历史快照，文件名含上线时间和版本号

备份就是复制这个目录。Linux 可加个定时任务：

```bash
# crontab -e，每天凌晨 2 点备份
0 2 * * * tar -czf /backup/capacity-$(date +\%Y\%m\%d).tar.gz /opt/capacity/data
```

数据出问题要回滚：停服务 → 从 `history/` 里挑一个快照覆盖成 `state.json` → 启服务。

---

## 安全提醒

服务**没有账号体系**，只有可选的单一口令。这意味着：

- 能访问端口的人都能读写全部数据，没有按组隔离权限
- 不要暴露到公网。如果服务器有公网 IP，务必让运维限制只允许内网访问
- 涉及人力编制数据，建议至少启用 `ACCESS_TOKEN`

真要做到「每个组只能改自己那几行」，需要加登录和权限体系，工作量比现在这套大不少。以团队内部协作的信任度来说，当前方案够用。

---

## 常见问题

**同事说打不开** — 先自己在服务器上 `curl http://localhost:端口` 确认服务活着，再查防火墙，最后确认对方在同一内网。

**怎么停止服务** — 在运行窗口按 `Ctrl+C`。服务会先给所有在线页面推一条通知再退出，各端立刻显示橙色横幅「服务已停止，填写只存本机」，不会让人蒙在鼓里继续填。装成 systemd/nssm 服务的用 `systemctl stop capacity` / `nssm stop Capacity`，效果一样。

**停服期间别人填的会丢吗** — 不会。断连后每次改动写入对方浏览器暂存，服务重启后页面自动重连并补交。提醒他们别关浏览器、别清缓存、别用无痕窗口；不放心可以点横幅上的「导出 JSON 备份」。

**多人在停服期间都填了，恢复后会互相覆盖吗** — 不会。恢复时做三方合并：按组、按条目逐项比对，A 填的组和 B 填的组都保留。只有两人改了同一格才提示核对，且默认保留本人的值，两边内容都不丢。

**两人同时改会冲突吗** — 服务正常时每次改动秒级同步，几乎撞不上；真撞上也是自动合并，不是二选一。

**升级代码怎么做** — 替换 js/css/html 文件，重启服务。`data` 目录不要动，数据格式向后兼容。

**服务器重启后要手动启动吗** — 按第 4 步配了 systemd 或 nssm 就会自动起。没配的话要手动。

**数据文件坏了怎么办** — 服务启动时若发现 `state.json` 无法解析，会自动回退到 `history/` 里版本号最大的可用快照，并在日志里说明。若连快照都没有，原文件会另存为 `state.json.broken` 供人工检查。

**回归测试** — 改过代码后可跑 `node _test-merge.js`（合并逻辑）和 `node _test-server.js`（服务端行为）。两个 `_` 开头的文件是测试用，部署时可以不传。
